<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Supermarket App</title>

  <style>
    body {
      background: linear-gradient(135deg, #f7f8fc, #dfe7fd);
      min-height: 100vh;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    .welcome-box {
      margin-top: 100px;
      background: white;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0px 4px 20px rgba(0,0,0,0.08);
      text-align: center;
      transition: 0.3s ease-in-out;
    }

    .welcome-box:hover {
      transform: translateY(-3px);
      box-shadow: 0px 6px 25px rgba(0,0,0,0.1);
    }

    .welcome-title {
      font-size: 2.2rem;
      font-weight: 700;
      color: #343a40;
    }

    .welcome-subtitle {
      font-size: 1.2rem;
      margin-top: 15px;
      color: #6c757d;
    }

    .btn-custom {
      margin-top: 25px;
      padding: 12px 25px;
      font-size: 1rem;
      border-radius: 30px;
    }
  </style>
</head>

<body>
  <!-- Main Content -->
  <div class="container">
    <div class="welcome-box mx-auto col-md-6">
      <% if (user) { %>

        <h1 class="welcome-title">Welcome back, <%= user.username %>! 👋</h1>
        <p class="welcome-subtitle">
          You are logged in as <strong><%= user.role %></strong>.
        </p>

        <% if (user.role === "admin") { %>
          <a href="/inventory" class="btn btn-primary btn-custom">Go to Inventory</a>
        <% } else { %>
          <a href="/shopping" class="btn btn-success btn-custom">Start Shopping 🛒</a>
        <% } %>

      <% } else { %>

        <h1 class="welcome-title">Welcome to Supermarket App 🛍️</h1>
        <p class="welcome-subtitle">
          Please login or register to start shopping.
        </p>

        <a href="/login" class="btn btn-primary btn-custom me-2">Login</a>
        <a href="/register" class="btn btn-outline-secondary btn-custom">Register</a>

      <% } %>
    </div>
  </div>
  <%- include('partials/footer') %>
</body>
</html>