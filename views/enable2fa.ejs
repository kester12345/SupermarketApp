<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Enable Two-Factor Authentication</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        body {
            background: #eef2f7;
            font-family: "Segoe UI", sans-serif;
        }

        .wrapper {
            max-width: 650px;
            margin: 60px auto;
            animation: fadeIn .5s ease-in-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to   { opacity: 1; transform: translateY(0); }
        }

        .card-custom {
            background: #ffffff;
            padding: 40px;
            border-radius: 22px;
            box-shadow: 0 8px 22px rgba(0,0,0,0.10);
        }

        .title-icon {
            font-size: 42px;
            color: #0d6efd;
        }

        .section-title {
            font-size: 22px;
            font-weight: 700;
            margin-bottom: 15px;
        }

        code {
            background: #f0f3f8;
            padding: 10px 16px;
            border-radius: 10px;
            font-size: 18px;
            display: inline-block;
            margin-top: 5px;
        }

        .qr-box {
            padding: 20px;
            background: #fafbfd;
            border: 1px solid #eee;
            border-radius: 16px;
            text-align: center;
            margin-bottom: 25px;
        }

        .btn-custom {
            padding: 12px;
            font-size: 17px;
            border-radius: 12px;
            font-weight: 600;
        }

    </style>
</head>
<body>

<!-- NAVBAR -->
<%- include('partials/navbar', { user }) %>

<div class="wrapper">
    <div class="card-custom">

        <div class="text-center mb-3">
            <i class="bi bi-shield-lock-fill title-icon"></i>
            <h2 class="fw-bold mt-2">Enable Two-Factor Authentication</h2>
            <p class="text-muted">Add an extra layer of security to your account.</p>
        </div>

        <!-- STEP 1 -->
        <h4 class="section-title">
            <i class="bi bi-phone me-2"></i> Step 1 — Scan the QR Code
        </h4>
        <p>Open <strong>Google Authenticator</strong> or <strong>Microsoft Authenticator</strong> on your phone and scan the QR code below.</p>

        <div class="qr-box">
            <img src="<%= qrCode %>" width="220">
        </div>

        <div class="divider"></div>

        <!-- STEP 2 -->
        <h4 class="section-title">
            <i class="bi bi-key me-2"></i> Step 2 — Manual Code (Optional)
        </h4>
        <p>If you cannot scan the QR code, enter this code manually into your authenticator app:</p>

        <code><%= secret %></code>

        <div class="divider"></div>

        <!-- STEP 3 -->
        <h4 class="section-title">
            <i class="bi bi-lock me-2"></i> Step 3 — Verify Your 6-Digit Code
        </h4>

        <form action="/enable-2fa/verify" method="POST" class="mt-3">
            <label class="fw-semibold mb-1">Enter the 6-digit authentication code</label>
            <input type="text" name="token" maxlength="6"
                   placeholder="123456"
                   class="form-control form-control-lg mb-3"
                   required>

            <button class="btn btn-primary w-100 btn-custom">
                <i class="bi bi-shield-check me-2"></i> Enable 2FA
            </button>
        </form>

        <a href="/profile" class="btn btn-outline-secondary w-100 mt-3 btn-custom">
            <i class="bi bi-arrow-left"></i> Back to Profile
        </a>

    </div>
</div>

<%- include('partials/footer') %>

</body>
</html>